{
  "name": "DACS Community",
  "install": "python3 --version && python3 -c \"import sys; assert sys.version_info >= (3, 8), 'Python >= 3.8 required, found ' + sys.version\"",
  "repositoryDependencies": [
    "github.com/norgejbb-byte/DACS-Standard"
  ]
}
